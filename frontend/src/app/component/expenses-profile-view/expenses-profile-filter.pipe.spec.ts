import { ExpensesProfileFilterPipe } from './expenses-profile-filter.pipe';

describe('ExpensesProfileFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ExpensesProfileFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
